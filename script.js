const toggle=document.querySelector('.menu'),nav=document.getElementById('nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const o=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',o)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const form=document.getElementById('contactForm'),statusEl=document.getElementById('formStatus'),submitButton=document.getElementById('submitButton');if(form){form.addEventListener('submit',async e=>{e.preventDefault();statusEl.textContent='';statusEl.className='status';if(!form.checkValidity()){form.reportValidity();return}submitButton.disabled=true;submitButton.textContent='Enviando...';try{const data=Object.fromEntries(new FormData(form).entries());const r=await fetch('/api/contacto',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});let result={};try{result=await r.json()}catch{}if(!r.ok)throw new Error(result.error||'No se pudo enviar la consulta.');statusEl.textContent='Consulta enviada correctamente. Nos pondremos en contacto contigo.';statusEl.classList.add('success');form.reset()}catch(err){statusEl.textContent=err.message||'No se pudo enviar. Escríbenos por WhatsApp.';statusEl.classList.add('error')}finally{submitButton.disabled=false;submitButton.textContent='Enviar consulta'}})}
